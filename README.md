# Object Detection Using Color (OpenCV)

Simple color-based object detection using OpenCV in Python.

## Requirements

- Python 3.x
- OpenCV (`opencv-python`)
- NumPy

## Run

```bash
python app.py
```

## Notes

- Adjust HSV/range thresholds in `app.py` to match your target color and lighting.
